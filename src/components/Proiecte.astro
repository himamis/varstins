---
import ProiecteEntry from "./ProiecteEntry.astro";
import colors from "../colors.json";

import { getLangFromUrl, useTranslations } from "../i18n/utils";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

import image1 from "../assets/images/1-256.jpg";
import image2 from "../assets/images/2-256.jpg";
import image3 from "../assets/images/3-256.jpg";
import image4 from "../assets/images/4-256.jpg";
---

<div id="proiecte" class="section is-medium">
  <div class="container">
    <h1 class="title has-text-centered is-uppercase">Proiecte</h1>
    <div class="columns mt-6">
      <div class="column">
        <ProiecteEntry
          title={t("proiecte.p1.title")}
          location={t("proiecte.p1.location")}
          image={image1}
          color={colors["red"]}
        />
      </div>
      <div class="column">
        <ProiecteEntry
          title={t("proiecte.p2.title")}
          location={t("proiecte.p2.location")}
          image={image2}
          color={colors["green"]}
        />
      </div>
      <div class="column">
        <ProiecteEntry
          title={t("proiecte.p3.title")}
          location={t("proiecte.p3.location")}
          image={image3}
          color={colors["blue"]}
        />
      </div>
      <div class="column">
        <ProiecteEntry
          title={t("proiecte.p4.title")}
          location={t("proiecte.p4.location")}
          image={image4}
          color={colors["yellow"]}
        />
      </div>
    </div>
  </div>
</div>
